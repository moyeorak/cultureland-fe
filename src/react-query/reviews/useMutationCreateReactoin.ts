// import api from "@/api/index.api";
// import { useMutation, useQueryClient } from "@tanstack/react-query";

export default function useMutationCreateReaction(reviewId: number) {
  // const queryClient = useQueryClient();
  // const mutationFn = (reactionData) =>
  //   api.reviews.createReactionInReview(reviewId, reactionData);
  // return useMutation({
  //   mutationFn,
  //   onSuccess: () => {
  //     queryClient.invalidateQueries({ exact: true, queryKey: ["reviews"] });
  //   },
  // });
}
